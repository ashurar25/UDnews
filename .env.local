

# Database Configuration - ใช้ Render PostgreSQL เท่านั้น
DATABASE_URL=${DATABASE_URL}

# OpenWeather API Configuration
OPENWEATHER_API_KEY=${OPENWEATHER_API_KEY}

# Environment Configuration
NODE_ENV=${NODE_ENV}

# Admin Configuration
ADMIN_USERNAME=${ADMIN_USERNAME}
ADMIN_PASSWORD=${ADMIN_PASSWORD}
JWT_SECRET=${JWT_SECRET}

# VAPID Keys for Push Notifications
VAPID_SUBJECT=${VAPID_SUBJECT}
VAPID_PUBLIC_KEY=${VAPID_PUBLIC_KEY}
VAPID_PRIVATE_KEY=${VAPID_PRIVATE_KEY}

# SMTP Configuration for Email Notifications
SMTP_HOST=${SMTP_HOST}
SMTP_USER=${SMTP_USER}
SMTP_PASS=${SMTP_PASS}
SMTP_FROM=${SMTP_FROM}
ADMIN_EMAIL=${ADMIN_EMAIL}
BASE_URL=${BASE_URL}

# หมายเหตุ: ระบบใช้ PostgreSQL จาก Render.com 
# - เซิร์ฟเวอร์ตั้งอยู่ที่ Singapore
# - ห้ามใช้ PostgreSQL ของ Replit
# - ค่า Environment Variables จะถูกโหลดจาก Replit Secrets

